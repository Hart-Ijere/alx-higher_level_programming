#!/usr/bin/node

// Retrieve the first and second arguments passed
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Print the output in the format: "<arg1> is <arg2>"
console.log(`${arg1} is ${arg2}`);
