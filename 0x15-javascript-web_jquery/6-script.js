// Use jQuery to select the DIV with the ID 'update_header' and bind a click event handler
$('#update_header').click(() => {
  // Update the text of the <header> element to "New Header!!!"
  $('header').text('New Header!!!')
})
