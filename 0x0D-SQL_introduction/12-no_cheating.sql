-- Task: Update the score of Bob to 10 in the table second_table from the database hbtn_0c_0
-- A query that updates the score to 10 for the row where the name is 'Bob'.

UPDATE second_table SET score = 10 WHERE name = 'Bob';
