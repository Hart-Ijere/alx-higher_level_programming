#!/usr/bin/python3
def islower(c):
    """ check for a lower case character."""
    
    if ord(c) in range(97,123):
        print("{} is lower".format(ord(c)))
    else:
        print("{} is upper".format(ord(c)))
